<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type opentelemetry

  endpoint otel-collector:4318

  service_name fluentd-forwarder
  service_namespace observability

  <span_attributes>
    "host.name" "#{Socket.gethostname}"
  </span_attributes>
</match>